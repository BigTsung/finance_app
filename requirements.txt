# Direct dependencies of scripts/finance_app.py only.
# Transitive packages (urllib3, certifi, idna, ...) are resolved by pip --
# do not re-add them from `pip freeze`.
beautifulsoup4>=4.12.3
pandas>=2.2.3
PyQt5>=5.15.10
PyQtChart>=5.15.6
requests>=2.32.4
twstock>=1.4.0
yfinance>=0.2.50
