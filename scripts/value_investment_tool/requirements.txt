# Direct dependencies of the value investment tool only.
# Transitive packages (urllib3, certifi, soupsieve, ...) are resolved by pip --
# do not re-add them from `pip freeze`.
matplotlib>=3.9.0
numpy>=2.0.2
pandas>=2.2.3
scipy>=1.13.0
yfinance>=0.2.50
